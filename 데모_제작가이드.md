# InterSteller 데모 제작 가이드 (v3)

> **목표:** 발표장에서 실제처럼 작동하는 목업. **API 0원, 인터넷 0 의존.**
> **스택:** HTML + CSS + Vanilla JS + Chart.js (CDN 1회 다운로드 후 로컬 캐시)
> **소요시간:** 3일 (Day 2~4 사용)
> **참고:** 본 가이드는 `최종기획서_v3.md` 의 시나리오 A/B 와 1:1 매핑

---

## 0. 데모가 보여줘야 하는 것

심사위원 머릿속에 남아야 할 임팩트 3개:

1. **앱 안의 행원** — 펀드 가입 직전 모달에서 "AI 행원과 통화" 선택 → 전화벨 → 김민지가 말한다
2. **통화-화면 동기** — 김민지가 *"그래프로 보여드릴게요"* 라고 말하면 동시에 앱 화면에 손실 시뮬레이션 차트가 풀스크린으로 강조됨
3. **두 갈래, 하나의 관문** — ARS 거절해도 마지막에 동일한 게이트웨이가 *"진짜 가입하시겠습니까?"* 를 묻는다

이 3개가 1분 30초 안에 시연되어야 한다.

---

## 1. 파일 구조

```
demo/
├── index.html              ← 발표용 시나리오 선택 (또는 펀드 메뉴)
├── scenario-a.html         ← ARS 승인 풀 흐름
├── scenario-b.html         ← ARS 거절 풀 흐름
├── style.css               ← 공통 스타일 (우리은행 톤)
├── app.js                  ← 공통 JS (음성·차트·서명 유틸)
└── assets/
    ├── audio/              ← 사전 녹음 mp3 (없으면 Web Speech API 폴백)
    │   ├── 01-greeting.mp3       ← 인사
    │   ├── 02-recommend.mp3      ← 펀드 추천
    │   ├── 03-explain-ter.mp3    ← TER 설명
    │   ├── 04-explain-loss.mp3   ← 손실 가능성 설명
    │   ├── 05-gateway.mp3        ← 게이트웨이 음성 요약
    │   └── ringtone.mp3          ← 전화벨 (5초)
    ├── images/
    │   ├── kim-minji.png         ← AI 행원 아바타 (원형 일러스트)
    │   └── woori-logo.svg
    └── data/
        └── customer.json         ← 박영희 프로필 더미
```

> **빌드 도구 0개.** 브라우저에서 `index.html` 더블클릭으로 열림. Chart.js만 CDN 1회 다운로드 후 `assets/lib/chart.min.js` 로 저장해 로컬 로드.

---

## 2. 화면 0 — index.html (발표용 진입점)

발표 중 시연이 부드럽게 흐르도록 **시나리오 선택 버튼**만 둔다. 본선 환경에선 발표자가 마우스 한 번으로 분기.

```
┌─────────────────────────────────────┐
│            InterSteller              │
│   행원처럼 설명합니다. 당신 편에서만.   │
│                                     │
│   ── 데모 시나리오 선택 ──            │
│                                     │
│   [ A. ARS 승인 — 음성 상담 ]       │
│   [ B. ARS 거절 — 자율 탐색 ]       │
│                                     │
│   * 박영희 (62세) 펀드 가입 시나리오  │
└─────────────────────────────────────┘
```

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <title>InterSteller — 우리은행 AI 행원</title>
  <link rel="stylesheet" href="style.css">
</head>
<body class="entry">
  <main class="entry-card">
    <h1 class="brand">InterSteller</h1>
    <p class="slogan">행원처럼 설명합니다. 당신 편에서만.</p>
    <div class="entry-buttons">
      <a href="scenario-a.html" class="btn btn-primary">A. ARS 승인 — 음성 상담</a>
      <a href="scenario-b.html" class="btn btn-ghost">B. ARS 거절 — 자율 탐색</a>
    </div>
    <p class="entry-note">박영희 (62세) · 펀드 가입 시나리오</p>
  </main>
</body>
</html>
```

---

## 3. 화면 A — scenario-a.html (ARS 승인 풀 흐름)

### 3.1 전체 흐름 (단일 페이지 내 상태 전환)

```
[A-1] 펀드 상세 페이지 (단기채권 펀드)
        ↓ "가입하기" 클릭
[A-2] ARS 동의 모달 — "AI 행원과 통화하시겠습니까?"
        ↓ "네, 전화 받을게요" 클릭
[A-3] 콜백 안내 — "30초 이내 전화드립니다"
        ↓ 3초 후 (데모용 단축)
[A-4] 전화 수신 화면 — 진동 + 벨소리 + 받기 버튼
        ↓ "받기"
[A-5] 통화 화면 — AI 발화 시퀀스 + 화면 동기 시각자료
        ↓ "가입할게요" 발화 시뮬레이션 버튼
[A-6] 가입 직전 게이트웨이 — 단점 3종 시각화 + "진짜 가입하시겠습니까?"
        ↓ "가입 진행"
[A-7] 자필 서명 + 완료 화면
```

전부 한 페이지 안에서 `<section class="step" data-step="N">` 단위로 표시/숨김.

### 3.2 A-1. 펀드 상세 페이지

```
┌─────────────────────────────────────┐
│  우리WON뱅킹                 🔔 👤    │
├─────────────────────────────────────┤
│  📊 우리 단기채권 펀드                │
│  ─────────────────────              │
│  연 기대수익  3.5%                   │
│  위험등급    ●●●○○ (3/5)             │
│  운용보수    연 0.4%                 │
│  최소 가입금액  10만원               │
│                                     │
│  [ KIID 보기 ]    [ 약관 보기 ]      │
│                                     │
│           [ 가입하기 ]               │
└─────────────────────────────────────┘
```

```html
<section class="step active" data-step="1">
  <header class="app-header">
    <img src="assets/images/woori-logo.svg" alt="우리은행">
    <span>우리WON뱅킹</span>
  </header>
  <article class="fund-card">
    <h2>📊 우리 단기채권 펀드</h2>
    <dl class="fund-meta">
      <dt>연 기대수익</dt><dd>3.5%</dd>
      <dt>위험등급</dt><dd>●●●○○ (3/5)</dd>
      <dt>운용보수</dt><dd>연 0.4%</dd>
      <dt>최소 가입금액</dt><dd>10만원</dd>
    </dl>
    <div class="fund-doc-buttons">
      <button class="btn btn-ghost">KIID 보기</button>
      <button class="btn btn-ghost">약관 보기</button>
    </div>
    <button class="btn btn-primary btn-block" onclick="goStep(2)">가입하기</button>
  </article>
</section>
```

### 3.3 A-2. ARS 동의 모달

```
┌─────────────────────────────────────┐
│   💬 AI 행원 음성 상담                │
│                                     │
│   펀드는 원금 손실이 가능한 상품이라    │
│   AI 행원이 직접 안내드릴 수 있어요.   │
│                                     │
│   "안내 받으시겠습니까?"               │
│                                     │
│   [ 네, 전화 받을게요 ]               │
│   [ 아니요, 혼자 볼게요 ]             │
└─────────────────────────────────────┘
```

```html
<section class="step modal" data-step="2">
  <div class="modal-card">
    <h3>💬 AI 행원 음성 상담</h3>
    <p>펀드는 원금 손실이 가능한 상품이라<br>AI 행원이 직접 안내드릴 수 있어요.</p>
    <p class="modal-q">"안내 받으시겠습니까?"</p>
    <button class="btn btn-primary" onclick="goStep(3)">네, 전화 받을게요</button>
    <button class="btn btn-ghost" onclick="location.href='scenario-b.html'">아니요, 혼자 볼게요</button>
  </div>
</section>
```

### 3.4 A-3. 콜백 안내

```html
<section class="step" data-step="3">
  <div class="callback-card">
    <div class="spinner"></div>
    <p>30초 이내<br><strong>02-2002-XXXX</strong><br>번호로 전화드리겠습니다.</p>
  </div>
</section>
```

```javascript
// goStep(3) 진입 시 3초 후 자동으로 A-4 진입 (데모 단축)
function onEnterStep3() {
  setTimeout(() => goStep(4), 3000);
}
```

### 3.5 A-4. 전화 수신 화면 (가장 임팩트 큰 장면)

```
┌─────────────────────────────────────┐
│                                     │
│         📞 전화 수신중...            │
│                                     │
│     ┌─────────────────────┐         │
│     │   [김민지 원형 사진]  │         │
│     │                     │         │
│     │     김민지           │         │
│     │  우리은행 AI 안내사    │         │
│     │  02-2002-XXXX        │         │
│     └─────────────────────┘         │
│                                     │
│       🔴 거절     🟢 받기            │
└─────────────────────────────────────┘
```

```html
<section class="step incoming-call" data-step="4">
  <p class="incoming-label">📞 전화 수신중...</p>
  <div class="caller-card">
    <img src="assets/images/kim-minji.png" class="avatar shake" alt="">
    <h3>김민지</h3>
    <p>우리은행 AI 안내사</p>
    <p class="caller-num">02-2002-XXXX</p>
  </div>
  <div class="call-actions">
    <button class="btn-call decline">🔴 거절</button>
    <button class="btn-call accept" onclick="acceptCall()">🟢 받기</button>
  </div>
  <audio id="ringtone" src="assets/audio/ringtone.mp3" loop></audio>
</section>
```

```javascript
function onEnterStep4() {
  document.getElementById('ringtone').play();
  // .shake CSS 클래스로 아바타 좌우 진동 (1초 주기)
}

function acceptCall() {
  document.getElementById('ringtone').pause();
  goStep(5);
}
```

```css
.shake { animation: shake 0.6s infinite; }
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-6px); }
  75% { transform: translateX(6px); }
}
```

### 3.6 A-5. 통화 화면 (핵심)

**좌측: 통화 정보 + 발화 자막. 우측: 시각자료 카드 (발화에 맞춰 강조).**

```
┌──────────────────┬──────────────────┐
│  📞 통화중 02:14  │  📊 펀드 정보      │
│                  │                  │
│  [김민지 아바타]   │  ┌────────────┐  │
│                  │  │ 위험 게이지   │  │
│  AI: "1,000만원  │  │ ●●●○○ 3/5  │  │
│   투자하시면      │  └────────────┘  │
│   손실 가능성을   │                  │
│   그래프로        │  ┌────────────┐  │
│   보여드릴게요"   │  │ 손실 시뮬     │  │
│                  │  │ [차트]      │  │
│  [💬 사용자 발화] │  │ 강조됨 ★   │  │
│  [추천해 주세요]  │  └────────────┘  │
│  [TER이 뭐예요?] │                  │
│  [원금 손실 가능?]│  ┌────────────┐  │
│  [가입할게요]    │  │ 누적 수수료   │  │
│                  │  └────────────┘  │
│  ☎ 통화 종료     │                  │
└──────────────────┴──────────────────┘
```

#### 좌측 — 통화 패널

```html
<section class="step call-screen" data-step="5">
  <div class="call-panel">
    <div class="call-status">
      📞 통화중 <span id="callTimer">00:00</span>
    </div>
    <img src="assets/images/kim-minji.png" class="avatar avatar-lg" alt="">
    <div class="caption" id="aiCaption">
      <!-- AI 발화 자막. 음성 재생과 동기 -->
    </div>
    <div class="user-utterances">
      <p class="hint">💬 사용자 발화 시뮬레이션</p>
      <button class="btn-utter" data-utter="recommend">추천해 주세요</button>
      <button class="btn-utter" data-utter="ter">TER이 뭐예요?</button>
      <button class="btn-utter" data-utter="loss">원금 손실 진짜 가능해요?</button>
      <button class="btn-utter" data-utter="signup">가입할게요</button>
    </div>
    <button class="btn-end-call" onclick="endCall()">☎ 통화 종료</button>
  </div>

  <div class="viz-panel">
    <h4>📊 펀드 정보</h4>
    <div class="viz-card" id="vizRisk">
      <h5>위험 게이지</h5>
      <canvas id="riskGauge"></canvas>
    </div>
    <div class="viz-card" id="vizLoss">
      <h5>손실 시뮬레이션 (1,000만원)</h5>
      <canvas id="lossChart"></canvas>
    </div>
    <div class="viz-card" id="vizFee">
      <h5>5년 누적 수수료</h5>
      <canvas id="feeChart"></canvas>
    </div>
    <div class="viz-card" id="vizTimeline">
      <h5>환매 타임라인</h5>
      <canvas id="timelineChart"></canvas>
    </div>
  </div>
</section>
```

#### 발화 시퀀스 데이터

```javascript
const AI_UTTERANCES = {
  greeting: {
    text: "안녕하세요, 박영희 고객님. 우리은행 AI 안내사 김민지입니다. " +
          "펀드 상품을 살펴보고 계시네요. 편하게 통화하시면서 궁금한 점 말씀해 주세요. " +
          "어떤 펀드를 고려 중이세요? 아니면 제가 고객님께 맞는 펀드를 추천해드릴까요?",
    audio: "assets/audio/01-greeting.mp3",
    focusViz: null
  },
  recommend: {
    text: "네, 박영희 고객님은 적금 중심으로 거래해오셨고, 펀드는 처음이시네요. " +
          "그러면 위험이 낮은 채권형 펀드부터 말씀드릴게요. " +
          "'우리 단기채권 펀드'는 연 3.5% 안팎 수익이 기대됩니다. " +
          "다만 짚어드릴 점이 있어요. 화면의 위험 게이지를 봐주실래요?",
    audio: "assets/audio/02-recommend.mp3",
    focusViz: "vizRisk"
  },
  ter: {
    text: "TER은 펀드 운영에 드는 모든 수수료를 합친 비율인데요, " +
          "1,000만원 가입하시면 매년 약 12만원이 수수료로 빠져나가요. " +
          "5년이면 약 60만원입니다. 화면의 수수료 차트를 보시죠.",
    audio: "assets/audio/03-explain-ter.mp3",
    focusViz: "vizFee"
  },
  loss: {
    text: "네, 가능합니다. 채권 금리가 갑자기 오르면 펀드 가격이 떨어져요. " +
          "2022년에 채권형 펀드 가입자 다수가 5~10% 손실을 본 사례가 있어요. " +
          "1,000만원이 어떻게 변동될 수 있는지 그래프로 보여드릴게요.",
    audio: "assets/audio/04-explain-loss.mp3",
    focusViz: "vizLoss"
  },
  signup_response: {
    text: "네, 알겠습니다. 그러면 가입 직전에 단점 3가지를 다시 확인하는 화면으로 " +
          "이어드릴게요. 마지막 확인 부탁드려요.",
    audio: "assets/audio/05-gateway.mp3",
    focusViz: null,
    next: () => goStep(6)
  }
};
```

#### 발화 재생 + 화면 동기 핵심 로직

```javascript
let callStartTime = null;

function onEnterStep5() {
  callStartTime = Date.now();
  startCallTimer();
  initAllCharts();
  playAI('greeting');
}

function startCallTimer() {
  setInterval(() => {
    if (!callStartTime) return;
    const sec = Math.floor((Date.now() - callStartTime) / 1000);
    const mm = String(Math.floor(sec / 60)).padStart(2, '0');
    const ss = String(sec % 60).padStart(2, '0');
    document.getElementById('callTimer').textContent = `${mm}:${ss}`;
  }, 1000);
}

function playAI(key) {
  const u = AI_UTTERANCES[key];
  if (!u) return;

  // 자막 표시
  document.getElementById('aiCaption').textContent = u.text;

  // 시각자료 강조 (CSS 클래스 토글)
  document.querySelectorAll('.viz-card').forEach(el => el.classList.remove('focus'));
  if (u.focusViz) {
    document.getElementById(u.focusViz).classList.add('focus');
    // 모바일에서 자동 스크롤
    document.getElementById(u.focusViz).scrollIntoView({ behavior: 'smooth', block: 'center' });
  }

  // 음성 재생 (사전 녹음 우선, 없으면 Web Speech API 폴백)
  const audio = new Audio(u.audio);
  audio.play().catch(() => speakWithWebAPI(u.text));

  // audit log
  logEvent('ai_utterance', { key, ts: Date.now() });

  // 후속 액션 (signup_response → 게이트웨이로)
  if (u.next) {
    audio.addEventListener('ended', u.next);
  }
}

// 사용자 발화 버튼 핸들러
document.querySelectorAll('.btn-utter').forEach(btn => {
  btn.addEventListener('click', () => {
    const key = btn.dataset.utter;
    // 사용자 발화 자막 잠깐 표시
    showUserCaption(btn.textContent);
    logEvent('user_utterance', { key, ts: Date.now() });

    const replyKey = key === 'signup' ? 'signup_response' : key;
    setTimeout(() => playAI(replyKey), 800);
  });
});

function speakWithWebAPI(text) {
  if (!('speechSynthesis' in window)) return;
  const u = new SpeechSynthesisUtterance(text);
  u.lang = 'ko-KR';
  u.rate = 0.95;
  u.pitch = 1.0;
  // 한국어 여성 음성 자동 선택
  const voice = speechSynthesis.getVoices().find(v => v.lang === 'ko-KR' && /female|여/i.test(v.name));
  if (voice) u.voice = voice;
  speechSynthesis.speak(u);
}
```

#### 시각자료 강조 CSS

```css
.viz-card {
  border: 1px solid #E0E6F0;
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 16px;
  background: #fff;
  transition: all 0.4s ease;
  opacity: 0.55;
}

.viz-card.focus {
  opacity: 1;
  border-color: #0046AD;
  box-shadow: 0 0 0 4px rgba(0, 70, 173, 0.15);
  transform: scale(1.02);
}
```

### 3.7 A-6. 가입 직전 게이트웨이 (시나리오 A·B 공통)

```
┌─────────────────────────────────────┐
│   📊 가입 직전 최종 확인               │
│                                     │
│   박영희 님, 단기채권 펀드             │
│   1,000만원 가입 전 3가지 확인.         │
│                                     │
│   1. 원금 손실 가능 (위험등급 3/5)     │
│      [위험 게이지]                    │
│                                     │
│   2. 5년 수수료 약 60만원              │
│      [누적 수수료 막대]                │
│                                     │
│   3. 환매 시 영업일 +2 후 입금          │
│      [타임라인]                       │
│                                     │
│   🔊 [음성으로 다시 듣기]              │
│                                     │
│   "정말 가입하시겠습니까?"              │
│                                     │
│   [ 잠시 보류 ]    [ 가입 진행 ]      │
└─────────────────────────────────────┘
```

```html
<section class="step gateway" data-step="6">
  <div class="gateway-header">
    <h3>📊 가입 직전 최종 확인</h3>
    <p>박영희 님, <strong>단기채권 펀드 1,000만원</strong> 가입 전<br>3가지를 다시 확인해 주세요.</p>
  </div>

  <ol class="gateway-checks">
    <li>
      <h4>1. 원금 손실 가능 (위험등급 3/5)</h4>
      <canvas id="gwRisk"></canvas>
    </li>
    <li>
      <h4>2. 5년간 수수료 약 60만원 차감</h4>
      <canvas id="gwFee"></canvas>
    </li>
    <li>
      <h4>3. 환매 시 영업일 +2 후 입금</h4>
      <canvas id="gwTimeline"></canvas>
    </li>
  </ol>

  <button class="btn btn-ghost" onclick="replayGatewaySummary()">🔊 음성으로 다시 듣기</button>

  <p class="gateway-q">"정말 가입하시겠습니까?"</p>

  <div class="gateway-actions">
    <button class="btn btn-ghost" onclick="onHold()">잠시 보류</button>
    <button class="btn btn-primary" onclick="goStep(7)">가입 진행</button>
  </div>
</section>
```

### 3.8 A-7. 자필 서명 + 완료

```html
<section class="step" data-step="7">
  <h3>✍ 자필 서명</h3>
  <p>아래 영역에 서명해 주세요.</p>
  <canvas id="signatureCanvas" width="320" height="120"></canvas>
  <div>
    <button class="btn btn-ghost" onclick="clearSignature()">다시 서명</button>
    <button class="btn btn-primary" onclick="completeSignup()">가입 완료</button>
  </div>
  <div class="hidden" id="completePanel">
    <h2>✅ 가입이 완료되었습니다</h2>
    <p>박영희 님의 단기채권 펀드 1,000만원 가입이 정상 처리되었습니다.</p>
    <button class="btn btn-ghost" onclick="showAuditLog()">audit log 보기 (심사위원용)</button>
  </div>
</section>
```

---

## 4. 화면 B — scenario-b.html (ARS 거절 자율 탐색)

### 4.1 흐름

```
[B-1] 펀드 목록 (단기채권 / 글로벌 주식형 4호 등)
        ↓ "글로벌 주식형 펀드 4호" 클릭 (위험 5/5)
[B-2] 펀드 상세 페이지 (장점 위주, 일반적)
        ↓ "가입하기" 클릭
[B-3] ARS 동의 모달 — "아니요, 혼자 볼게요"
        ↓
[B-4] 가입 직전 게이트웨이 (강제 진입)
       - 적합성 점수 2/5
       - 단점 3가지 시각화
       - 🔊 [지금이라도 AI 행원 통화하기] ← 시나리오 A 합류 동선
        ↓ "잠시 보류" 또는 "가입 진행"
[B-5] 보류 화면 (대부분 여기서 종결) 또는 가입 진행
```

### 4.2 핵심 게이트웨이 (B만의 특이점)

```
┌─────────────────────────────────────┐
│   ⚠ 박영희 님께는                     │
│   "주의가 필요한 상품"입니다.           │
│                                     │
│   적합성 점수: ●●○○○ (2/5)            │
│                                     │
│   이런 점들이 박영희 님 상황과         │
│   맞지 않을 수 있어요:                 │
│                                     │
│   1. 위험등급 5/5 (최고 위험)         │
│      거래 이력상 첫 고위험 가입         │
│      [위험 게이지]                    │
│                                     │
│   2. 글로벌 주식 변동성               │
│      1,000만 → 300~1,500만 변동       │
│      [손실 시뮬레이션 그래프]          │
│                                     │
│   3. 최근 의료비 1,200만원 지출        │
│      비상금 부족 가능성                │
│      [자금 흐름]                      │
│                                     │
│   🔊 [지금이라도 AI 행원 통화하기]    │
│                                     │
│   "정말 가입하시겠습니까?"              │
│                                     │
│   [ 잠시 보류 ]    [ 가입 진행 ]      │
└─────────────────────────────────────┘
```

```html
<button class="btn btn-ghost" onclick="location.href='scenario-a.html?step=4'">
  🔊 지금이라도 AI 행원 통화하기
</button>
```

이 버튼이 **두 시나리오의 합류 지점**. 발표 때 한 번 누르면 시나리오 A의 전화 수신 화면(A-4)으로 이동.

---

## 5. 시각화 4종 — Chart.js 코드

CDN: `<script src="assets/lib/chart.min.js"></script>` (사전 다운로드 후 로컬)

### 5.1 위험 게이지 (도넛 차트로 구현)

```javascript
function renderRiskGauge(canvasId, level, max = 5) {
  const ctx = document.getElementById(canvasId);
  new Chart(ctx, {
    type: 'doughnut',
    data: {
      datasets: [{
        data: [level, max - level],
        backgroundColor: [level >= 4 ? '#D32F2F' : level >= 3 ? '#F9A825' : '#388E3C', '#E0E6F0'],
        borderWidth: 0
      }]
    },
    options: {
      circumference: 180,
      rotation: 270,
      cutout: '70%',
      plugins: {
        legend: { display: false },
        tooltip: { enabled: false }
      }
    },
    plugins: [{
      id: 'centerText',
      afterDraw(chart) {
        const { ctx, chartArea } = chart;
        ctx.save();
        ctx.font = 'bold 28px Pretendard';
        ctx.fillStyle = '#222';
        ctx.textAlign = 'center';
        ctx.fillText(`${level}/${max}`, (chartArea.left + chartArea.right) / 2, chartArea.bottom - 10);
        ctx.restore();
      }
    }]
  });
}
```

### 5.2 손실 시뮬레이션 (변동성 라인)

```javascript
function renderLossChart(canvasId, principal = 1000) {
  const ctx = document.getElementById(canvasId);
  const months = ['0','3','6','9','12','15','18','21','24','27','30','33','36'];
  // 시각적 임팩트용 — 큰 변동성 보여주기
  const optimistic = months.map((_, i) => principal * (1 + 0.035 * (i / 12)));
  const pessimistic = months.map((_, i) => principal * (1 - 0.08 * Math.sin(i * 0.4)));

  new Chart(ctx, {
    type: 'line',
    data: {
      labels: months,
      datasets: [
        { label: '기대 수익', data: optimistic, borderColor: '#388E3C', tension: 0.3 },
        { label: '손실 시나리오', data: pessimistic, borderColor: '#D32F2F', tension: 0.3 }
      ]
    },
    options: {
      scales: {
        y: { ticks: { callback: v => `${v}만원` } },
        x: { title: { display: true, text: '개월' } }
      },
      plugins: {
        legend: { position: 'bottom' }
      }
    }
  });
}
```

### 5.3 누적 수수료 (막대 차트)

```javascript
function renderFeeChart(canvasId) {
  const ctx = document.getElementById(canvasId);
  new Chart(ctx, {
    type: 'bar',
    data: {
      labels: ['1년', '2년', '3년', '4년', '5년'],
      datasets: [{
        label: '누적 수수료 (만원)',
        data: [12, 24, 36, 48, 60],
        backgroundColor: '#D32F2F'
      }]
    },
    options: {
      plugins: {
        legend: { display: false },
        tooltip: { callbacks: { label: ctx => `${ctx.parsed.y}만원` } }
      },
      scales: { y: { ticks: { callback: v => `${v}만원` } } }
    }
  });
}
```

### 5.4 환매 타임라인 (가로 막대로 표현)

```javascript
function renderTimelineChart(canvasId) {
  const ctx = document.getElementById(canvasId);
  new Chart(ctx, {
    type: 'bar',
    data: {
      labels: ['환매 신청', '심사', '입금'],
      datasets: [{
        data: [0, 1, 2],
        backgroundColor: ['#0046AD', '#F9A825', '#388E3C']
      }]
    },
    options: {
      indexAxis: 'y',
      plugins: {
        legend: { display: false },
        tooltip: { callbacks: { label: ctx => `D+${ctx.parsed.x} 영업일` } }
      },
      scales: {
        x: { ticks: { callback: v => `D+${v}` }, max: 3 }
      }
    }
  });
}
```

### 5.5 초기화 일괄 호출

```javascript
function initAllCharts() {
  renderRiskGauge('riskGauge', 3);
  renderLossChart('lossChart');
  renderFeeChart('feeChart');
  renderTimelineChart('timelineChart');
  // 게이트웨이용
  renderRiskGauge('gwRisk', 3);
  renderFeeChart('gwFee');
  renderTimelineChart('gwTimeline');
}
```

---

## 6. 자필 서명 (Canvas)

```javascript
function initSignature() {
  const canvas = document.getElementById('signatureCanvas');
  const ctx = canvas.getContext('2d');
  let drawing = false;

  const start = e => {
    drawing = true;
    const rect = canvas.getBoundingClientRect();
    ctx.beginPath();
    ctx.moveTo((e.clientX || e.touches[0].clientX) - rect.left,
               (e.clientY || e.touches[0].clientY) - rect.top);
  };
  const draw = e => {
    if (!drawing) return;
    e.preventDefault();
    const rect = canvas.getBoundingClientRect();
    ctx.lineWidth = 2.5;
    ctx.lineCap = 'round';
    ctx.strokeStyle = '#0046AD';
    ctx.lineTo((e.clientX || e.touches[0].clientX) - rect.left,
               (e.clientY || e.touches[0].clientY) - rect.top);
    ctx.stroke();
  };
  const end = () => { drawing = false; };

  canvas.addEventListener('mousedown', start);
  canvas.addEventListener('mousemove', draw);
  canvas.addEventListener('mouseup', end);
  canvas.addEventListener('touchstart', start);
  canvas.addEventListener('touchmove', draw);
  canvas.addEventListener('touchend', end);
}

function clearSignature() {
  const canvas = document.getElementById('signatureCanvas');
  canvas.getContext('2d').clearRect(0, 0, canvas.width, canvas.height);
}

function completeSignup() {
  logEvent('signup_completed', { ts: Date.now() });
  document.getElementById('completePanel').classList.remove('hidden');
}
```

---

## 7. audit log (콘솔 + localStorage)

EU AI Act Article 11 정합 — 모든 이벤트 시각 기록.

```javascript
const AUDIT_KEY = 'intersteller_audit_log';

function logEvent(type, payload) {
  const log = JSON.parse(localStorage.getItem(AUDIT_KEY) || '[]');
  log.push({
    type,
    payload,
    iso: new Date().toISOString()
  });
  localStorage.setItem(AUDIT_KEY, JSON.stringify(log));
  console.log('[audit]', type, payload);
}

function showAuditLog() {
  const log = JSON.parse(localStorage.getItem(AUDIT_KEY) || '[]');
  const win = window.open('', '_blank');
  win.document.write('<pre>' + JSON.stringify(log, null, 2) + '</pre>');
}
```

기록되는 이벤트:
- `step_enter` — 단계 진입 시각
- `ai_utterance` — AI 발화 키
- `user_utterance` — 사용자 발화 시뮬
- `viz_focus` — 시각자료 강조 시각
- `gateway_view` — 게이트웨이 노출 시간
- `signup_completed` / `held` — 최종 결정

---

## 8. 스타일 가이드

```css
:root {
  --woori-primary: #0046AD;
  --woori-primary-dark: #003087;
  --woori-bg-soft: #EBF3FF;
  --woori-bg-warn: #FFF8E1;
  --color-danger: #D32F2F;
  --color-warn: #F9A825;
  --color-success: #388E3C;
  --color-text: #1a1a1a;
  --color-muted: #6b7280;
  --radius-md: 12px;
  --radius-lg: 16px;
}

body {
  font-family: 'Pretendard', 'Apple SD Gothic Neo', sans-serif;
  margin: 0;
  background: #F5F7FB;
  color: var(--color-text);
  -webkit-font-smoothing: antialiased;
}

.btn {
  padding: 14px 20px;
  border-radius: var(--radius-md);
  font-size: 16px;
  font-weight: 600;
  border: none;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-primary {
  background: var(--woori-primary);
  color: #fff;
}

.btn-primary:hover { background: var(--woori-primary-dark); }

.btn-ghost {
  background: #fff;
  color: var(--woori-primary);
  border: 1.5px solid var(--woori-primary);
}

.btn-block { display: block; width: 100%; }
.hidden { display: none; }

.step { display: none; }
.step.active { display: block; }

/* 모바일 우선, 발표 시 데스크탑 풀스크린에서도 깔끔하게 */
.app-container {
  max-width: 420px;
  margin: 0 auto;
  background: #fff;
  min-height: 100vh;
}

/* 통화 화면은 데스크탑에서 좌우 분할 */
.call-screen {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
  max-width: 420px;
  margin: 0 auto;
}

@media (min-width: 900px) {
  .call-screen {
    max-width: 1100px;
    grid-template-columns: 380px 1fr;
  }
}
```

---

## 9. 3일 개발 일정 (Day 2~4)

### Day 2 (5/22) — 시나리오 A 골격

- [ ] 파일 구조 + index.html
- [ ] scenario-a.html 단계 1~4 (펀드 상세 → 모달 → 콜백 → 전화 수신)
- [ ] 전화 수신 진동 + ringtone.mp3
- [ ] 김민지 아바타 일러스트 (Midjourney·Figma Community 무료 에셋 활용)
- [ ] style.css 기본 토큰 확정

**EOD 기준:** 전화 받기 직전까지 흐름 시연 가능

### Day 3 (5/23) — 시나리오 A 통화·게이트웨이 + 시나리오 B

- [ ] 통화 화면 발화 시퀀스 4단계
- [ ] Chart.js 시각화 4종
- [ ] 발화-시각자료 동기 (`.focus` 클래스 토글)
- [ ] 사전 녹음 mp3 5개 (또는 Web Speech API)
- [ ] 게이트웨이 화면 (시나리오 A·B 공용 컴포넌트)
- [ ] 자필 서명 Canvas
- [ ] scenario-b.html 풀 흐름

**EOD 기준:** 두 시나리오 모두 끊김 없이 시연 가능

### Day 4 (5/24) — 마감 정리

- [ ] audit log 콘솔/팝업 출력
- [ ] 발표용 백업 데모 영상 녹화 (시나리오 A·B 각 1분 30초)
- [ ] 우리은행 색상·폰트 최종 정렬
- [ ] 모의 발표 2회
- [ ] 발표장 노트북에서 오프라인 재생 검증

---

## 10. 사전 녹음 음성 만들기

세 가지 옵션 중 선택:

| 옵션 | 품질 | 비용 | 권장도 |
|-----|-----|-----|-----|
| **A. 팀원이 직접 녹음** (스마트폰 + Audacity) | 중 | 0원 | 시간 없을 때 |
| **B. ElevenLabs 무료 크레딧 한국어 여성** | 상 | 무료 ~10분 | 추천 |
| **C. Web Speech API 런타임 합성** | 하 | 0원 | 폴백용 |

**B 옵션 권장 — 한 번만 합성해서 mp3로 저장. 본선 인터넷 불필요.**

사전 녹음 스크립트는 §3.6의 `AI_UTTERANCES` 텍스트 그대로 5~6개 파일로 합성.

> **김민지 톤 가이드:** 30대 후반 여성, 따뜻하지만 정중함. 시니어 대상이므로 평소 광고 톤보다 10% 천천히. 문장 끝을 살짝 올리지 말고 평탄하게.

---

## 11. 발표 시연 동선 (15분 중 4분)

### 시연 직전 준비 (S8 이후)s
- 노트북 전체화면, 브라우저 줌 150%
- 사이드 모니터에 audit log 콘솔 미리 띄움 (심사위원이 데이터 보임)
- localStorage 초기화 (`localStorage.removeItem('intersteller_audit_log')`)

### 시나리오 A 시연 (S9, 약 2분)
1. index.html → "A. ARS 승인" 클릭
2. 펀드 상세 페이지 5초 보여주기 → "가입하기"
3. 모달 등장 — *"AI 행원과 통화하시겠습니까?"* 강조 멘트
4. "네, 전화 받을게요" → 3초 콜백 안내
5. 전화 수신 화면 (벨소리 + 진동) — *"실제 통화처럼 보이도록 설계"* 멘트
6. "받기" → 통화 화면 진입
7. AI 인사 음성 자동 재생 (10초)
8. *"추천해 주세요"* 클릭 → AI가 *"위험 게이지를 봐주실래요"* → 화면 게이지가 풀 강조됨 (★ 핵심 임팩트)
9. *"원금 손실 진짜 가능?"* 클릭 → 손실 시뮬레이션 차트 강조
10. *"가입할게요"* 클릭 → 게이트웨이 화면 풀스크린

### 시나리오 B 시연 (S10, 약 1분 30초)
1. (S10 진입과 동시에) URL `scenario-b.html` 로 이동
2. 펀드 목록에서 글로벌 주식형 클릭
3. 일반 상세 페이지 → "가입하기"
4. 모달 → "아니요, 혼자 볼게요"
5. **곧바로 게이트웨이 강제 진입** (S11 핵심: *"통화를 거절해도 마지막 관문은 동일"*)
6. 적합성 점수 2/5 + 단점 3종 시각화
7. "잠시 보류" 클릭 → 종료

### 합류 시연 (S11, 30초)
- 시나리오 B의 *"🔊 지금이라도 AI 행원 통화하기"* 버튼 클릭
- 시나리오 A의 전화 수신 화면(A-4)으로 자연스럽게 합류
- *"두 갈래 어느 쪽이든 동일 보호 지점"* 멘트

### 마무리 (audit log 1탭)
- 새 탭에서 audit log JSON 출력 → 심사위원에게 *"발화·시각노출·결정 모두 기록"*

---

## 12. 완성 체크리스트

### 구조
- [ ] `demo/` 폴더 + 4개 html + style.css + app.js + assets/
- [ ] Chart.js 로컬 저장 (CDN 의존 0)
- [ ] 사전 녹음 mp3 5~6개

### 시나리오 A
- [ ] 펀드 상세 → 가입하기
- [ ] ARS 모달 → "네, 전화 받을게요"
- [ ] 콜백 안내 (3초 자동 진행)
- [ ] 전화 수신 (벨소리 + 진동 + 받기 버튼)
- [ ] 통화 화면 (자막 + 4가지 사용자 발화 버튼)
- [ ] 발화-시각자료 동기 (`.focus` 풀 강조)
- [ ] 게이트웨이 (3종 시각화 + "진짜 가입?")
- [ ] 자필 서명 + 가입 완료

### 시나리오 B
- [ ] 펀드 목록 → 글로벌 주식형 선택
- [ ] 일반 상세 → "가입하기"
- [ ] ARS 모달 → "아니요, 혼자 볼게요"
- [ ] 게이트웨이 강제 진입 (적합성 2/5)
- [ ] "지금이라도 AI 행원 통화하기" → 시나리오 A 합류

### 공통
- [ ] audit log localStorage 저장
- [ ] audit log 팝업 출력
- [ ] 우리은행 색상 일관성
- [ ] 백업 영상 녹화 (시나리오 A·B 각 1분 30초)
- [ ] 본선 노트북 오프라인 재생 검증

---

## 13. 데모에 일부러 넣지 않은 것 (보고서에만 기술)

| 항목 | 이유 |
|-----|-----|
| 실제 PSTN 통화 발신 | 통신사 계약 필요 + 5일 무리 |
| 실제 Whisper STT | 마이크 권한 + 정확도 변수. 사용자 발화 버튼이 더 깔끔 |
| 실제 Qwen3-TTS 추론 | GPU 필요. 사전 녹음 mp3로 동일 효과 |
| WebSocket 화면 동기 | 단일 페이지에선 setTimeout으로 충분 |
| RAG 검색 라이브 | 펀드 정보는 하드코딩. *"운영 시 우리GPT RAG"* 만 멘트 |

발표 멘트에 *"데모는 음성·통화망을 mock 처리. 실제 운영은 우리GPT + Qwen3-TTS + Whisper + PSTN. 5일 빌드 가능성과 운영 아키텍처의 분리는 §4·§9 참고"* 한 줄로 정리.

---

## 14. 트러블슈팅

| 증상 | 원인 / 해결 |
|-----|-----|
| Web Speech API 한국어 음성 없음 | Chrome/Edge 권장. Safari는 폴백 mp3 우선 재생 |
| Chart.js 차트가 안 그려짐 | `<canvas>` 가 `display: none` 상태에서 초기화하면 width 0. 단계 진입 후 `initAllCharts()` 호출 |
| mp3 자동재생 차단 | 첫 사용자 클릭(예: "가입하기") 이후엔 자동재생 허용됨 — 흐름상 자연스럽게 해결 |
| 본선 노트북 폰트 깨짐 | Pretendard 웹폰트 fallback에 `Apple SD Gothic Neo`, `Noto Sans KR`, `sans-serif` 명시 |
| 전화 진동 효과 약함 | `.shake` keyframes에서 translateX 폭을 8~10px로 키움 |

---

## 🎯 한 줄 요약

> **HTML 4장, JS 200줄, mp3 6개, Chart.js 4종. 인터넷·API·서버 0개로 InterSteller의 핵심 경험 — *"앱 안의 행원이 전화 걸어 단점도 말한다"* — 을 1분 30초 안에 완성한다.**
